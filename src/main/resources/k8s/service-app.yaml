apiVersion: v1
kind: Service
metadata:
  name: mechanical-hub-svc
  namespace: production
spec:
  type: LoadBalancer
  selector:
    app: mechanical-hub-api
  ports:
    - name: http
      port: 80
      targetPort: 8080